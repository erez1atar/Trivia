<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="games.android.trivia">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@drawable/icons"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icons"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GameActivity"
                  android:noHistory="true" />
        <activity
            android:name=".Stages.StagesPresentor"
            android:noHistory="true" />
        <activity android:name=".HighScores.HallOfFame" />
        <activity
            android:name=".ActivityGameResult"
            android:noHistory="true" />
        <activity
            android:name=".EnterNameActivity"
            android:noHistory="true" />
        <activity android:name=".Settings.SettingsActivity" />
        <activity android:name=".CreditsActivity"></activity>
    </application>

</manifest>